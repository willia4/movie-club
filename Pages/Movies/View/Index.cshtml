@page "{id}"
@model zinfandel_movie_club.Pages.Movies.View.Index

@{
    ViewData["Title"] = Model.MovieTitle;
}

<div>
    <h1>@Model.MovieTitle</h1>
    
    @if (!string.IsNullOrWhiteSpace(Model.CoverImageHref))
    {
        <img src="@Model.CoverImageHref" alt="movie cover"/>    
    }
</div>
